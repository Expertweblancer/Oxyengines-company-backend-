## Necessary commands for building this project
-----------------------------------------------

### To install dependency

#### `npm install` or `yarn`
#### `cp .env.example .env`
-----------------------------------------------

### To start development server
#### `node bin/server.js` (from the ROOT Folder)
-----------------------------------------------

